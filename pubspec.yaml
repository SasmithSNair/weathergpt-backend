name: weathergpt_frontend
description: WeatherGPT mobile client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  geolocator: ^13.0.1
  permission_handler: ^11.3.1
  speech_to_text: ^7.0.0
  flutter_tts: ^4.1.0
  shared_preferences: ^2.3.2
  network_info_plus: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logo.jpeg
